# ── Backend ServiceAccount ─────────────────────────────────────────────────────
# The backend pods run under this identity so the RBAC rules below apply.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gameserver-backend
  namespace: gameserver
