# ── Frontend Service (ClusterIP – reachable by nginx inside the cluster) ───────
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: gameserver
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
