# ── Backend Service (ClusterIP – reachable by nginx inside the cluster) ────────
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: gameserver
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
